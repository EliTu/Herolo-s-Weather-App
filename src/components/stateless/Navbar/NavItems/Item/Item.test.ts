import React from 'react';
import { shallow } from 'enzyme';
import Item from './Item';

describe('Item component', () => {
	let component;
	beforeEach(() => {
		component = shallow(<Item />);
	});
	it('should render without errors', () => {
        
    });
});
